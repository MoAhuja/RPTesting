import { Diagnostic, StringTemplate } from "../types.js";
/**
 * Convert a string template to a string value.
 * Only literal interpolated can be converted to string.
 * Otherwise diagnostics will be reported.
 *
 * @param stringTemplate String template to convert.
 */
export declare function stringTemplateToString(stringTemplate: StringTemplate): [string, readonly Diagnostic[]];
export declare function isStringTemplateSerializable(stringTemplate: StringTemplate): [boolean, readonly Diagnostic[]];
//# sourceMappingURL=string-template-utils.d.ts.map